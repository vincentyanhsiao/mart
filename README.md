# Mart Artwork System

## 本地启动基础指令

### 1. 数据库准备
执行 `sql/init.sql` 脚本初始化 MySQL 数据库。

### 2. 后端启动
```bash
cd backend
npm install
npm start
```

### 3. 前端访问
后端启动后，直接访问 `http://localhost:3000` 即可看到艺术品展示页面。
