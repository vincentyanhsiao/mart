{
  "name": "mart-artwork-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node backend/app.js",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
